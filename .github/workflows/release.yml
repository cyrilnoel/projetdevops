name: Release

on:
  push:
    tags:
      - '*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: '1.19'

    - name: Install dependencies
      run: go mod tidy

    - name: Configure git
      run: |
        git config --global user.email "cyrillekouamo@gmail.com.com"
        git config --global user.name "cyrilnoel"

    - name: Run Goreleaser
      uses: goreleaser/goreleaser-action@v2
      with:
        version: latest
        args: release

    - name: Build Docker image
      env:
        IMAGE_NAME: ${{ secrets.USERNAME_DOCKER }}/${{ github.event.repository.name }}:${{ github.ref_name }}
      run: |
        docker build -t $IMAGE_NAME .
    - name: Push Docker image
      env:
        DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      run: |
        echo $TOKEN_DOCKER | docker login -u $USERNAME_DOCKER --password-stdin
        docker push $IMAGE_NAME